// Toggle — Figma Component — Generated by figma-sync
// Execute via Figma Console MCP with the target file open

(async () => {
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  const colorCollection = collections.find(c => c.name === "Pylon/Colors");
  const colorCollectionId = colorCollection ? colorCollection.id : null;
  const radiiCollection = collections.find(c => c.name === "Pylon/Radii");

  // Helper: find a variable by name and collection
  async function findColorVar(name) {
    if (!colorCollectionId) return null;
    const vars = await figma.variables.getLocalVariablesAsync("COLOR");
    return vars.find(v => v.name === name && v.variableCollectionId === colorCollectionId) || null;
  }

  async function findFloatVar(name, collectionId) {
    if (!collectionId) return null;
    const vars = await figma.variables.getLocalVariablesAsync("FLOAT");
    return vars.find(v => v.name === name && v.variableCollectionId === collectionId) || null;
  }

  // Load font before creating text
  await figma.loadFontAsync({ family: "Inter", style: "Regular" });
  await figma.loadFontAsync({ family: "Inter", style: "Medium" });
  await figma.loadFontAsync({ family: "Inter", style: "Semi Bold" });
  await figma.loadFontAsync({ family: "Inter", style: "Bold" });

  const components = [];

  // Type=Pressed, Size=SM, State=Default
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Default";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=SM, State=Hover
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Hover";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=SM, State=Pressed
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Pressed";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=SM, State=Selected
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Selected";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=SM, State=Disabled
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Disabled";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    node.opacity = 0.5;
    components.push(node);
  }

  // Type=Pressed, Size=SM, State=Focused
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=SM, State=Focused";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 32);
    node.paddingTop = 4;
    node.paddingBottom = 4;
    node.paddingLeft = 12;
    node.paddingRight = 12;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    // Focus ring effect
    node.effects = [{
      type: "DROP_SHADOW",
      color: { r: 0.42, g: 0.545, b: 0.643, a: 1 },
      offset: { x: 0, y: 0 },
      radius: 0,
      spread: 4,
      visible: true,
      blendMode: "NORMAL"
    }];
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Default
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Default";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Hover
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Hover";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Pressed
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Pressed";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Selected
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Selected";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Disabled
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Disabled";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    node.opacity = 0.5;
    components.push(node);
  }

  // Type=Pressed, Size=MD, State=Focused
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=MD, State=Focused";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 38);
    node.paddingTop = 8;
    node.paddingBottom = 8;
    node.paddingLeft = 16;
    node.paddingRight = 16;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    // Focus ring effect
    node.effects = [{
      type: "DROP_SHADOW",
      color: { r: 0.42, g: 0.545, b: 0.643, a: 1 },
      offset: { x: 0, y: 0 },
      radius: 0,
      spread: 4,
      visible: true,
      blendMode: "NORMAL"
    }];
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Default
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Default";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Hover
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Hover";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Pressed
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Pressed";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Selected
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Selected";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Disabled
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Disabled";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    node.opacity = 0.5;
    components.push(node);
  }

  // Type=Pressed, Size=LG, State=Focused
  {
    const node = figma.createComponent();
    node.name = "Type=Pressed, Size=LG, State=Focused";
    node.layoutMode = "HORIZONTAL";
    node.primaryAxisSizingMode = "AUTO";
    node.counterAxisSizingMode = "AUTO";
    node.counterAxisAlignItems = "CENTER";
    node.primaryAxisAlignItems = "CENTER";
    node.itemSpacing = 8;
    node.counterAxisSizingMode = "FIXED";
    node.resize(node.width, 44);
    node.paddingTop = 12;
    node.paddingBottom = 12;
    node.paddingLeft = 24;
    node.paddingRight = 24;
    const radiusVar = await findFloatVar("radius/md", radiiCollection?.id);
    if (radiusVar) {
      node.setBoundVariable("topLeftRadius", radiusVar);
      node.setBoundVariable("topRightRadius", radiusVar);
      node.setBoundVariable("bottomLeftRadius", radiusVar);
      node.setBoundVariable("bottomRightRadius", radiusVar);
    }
    const fillVar = await findColorVar("color-surface");
    if (fillVar) {
      const fillPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", fillVar);
      node.fills = [fillPaint];
    }
    const strokeVar = await findColorVar("color-subtle");
    if (strokeVar) {
      const strokePaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", strokeVar);
      node.strokes = [strokePaint];
      node.strokeWeight = 1;
    }
    const text = figma.createText();
    text.fontName = { family: "Inter", style: "Regular" };
    text.fontSize = 13;
    text.characters = "Toggle";
    const textVar = await findColorVar("color-base");
    if (textVar) {
      const textPaint = figma.variables.setBoundVariableForPaint({ type: "SOLID", color: {r:0,g:0,b:0} }, "color", textVar);
      text.fills = [textPaint];
    }
    node.appendChild(text);
    // Focus ring effect
    node.effects = [{
      type: "DROP_SHADOW",
      color: { r: 0.42, g: 0.545, b: 0.643, a: 1 },
      offset: { x: 0, y: 0 },
      radius: 0,
      spread: 4,
      visible: true,
      blendMode: "NORMAL"
    }];
    components.push(node);
  }

  if (components.length > 1) {
    const componentSet = figma.combineAsVariants(components, figma.currentPage);
    componentSet.name = "Toggle";

    // Auto-layout the component set for organization
    componentSet.layoutMode = "HORIZONTAL";
    componentSet.layoutWrap = "WRAP";
    componentSet.itemSpacing = 24;
    componentSet.counterAxisSpacing = 24;
    componentSet.paddingTop = 40;
    componentSet.paddingBottom = 40;
    componentSet.paddingLeft = 40;
    componentSet.paddingRight = 40;
    componentSet.primaryAxisSizingMode = "AUTO";
    componentSet.counterAxisSizingMode = "AUTO";
  } else if (components.length === 1) {
    components[0].name = "Toggle";
  }

  figma.notify("✓ Toggle: 18 variants created");
  figma.closePlugin();
})();
